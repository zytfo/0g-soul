# Changelog

## v0.3 - Round of 16

### Added
- Custom characters - 8 presets plus a free-text "write your own"; the 0G image
  model (`qwen-image-edit`) themes each avatar from the personality text.
- Shareable Souls - a dynamic Open Graph card (avatar + name + character) for every
  `/agent/<id>` link, plus a one-tap "share to X" button.
- Public gallery at `/explore` - lists every Soul minted on 0G (read from the
  contract's `nextId`, not filtered by owner), each card showing its owner.
- Optional browser voice - speech-to-text dictation and text-to-speech replies,
  feature-detected and hidden where unsupported.
- Learned memory - at mint/update, 0G Compute distills a rolling summary and key
  facts about the user from the conversation and stores them in the 0G Storage
  memory blob (the memory panel fills with what the companion has learned).
- Séance - summon any two minted Souls and watch them hold a live, in-character
  conversation (each driven by its own personality + on-chain memory via 0G Compute),
  shareable at `/seance/<a>/<b>` with its own OG card.

### Changed
- Personality picker redesigned from a tall grid into compact, wrapping chips with
  tooltips and a live description line.
- Shared `lib/soul-meta.ts` now backs the NFT metadata route, the OG card, and the
  gallery (one place reads a Soul's display data).

## v0.2 - Round of 32

### Added
- AI avatars generated by 0G's image model, stored on 0G Storage, used as the NFT image.
- Streaming chat replies from 0G Compute (token-by-token).
- Transferable, real NFT - `SoulAgentV2` with `tokenURI`; the Soul renders as a proper
  NFT (name + avatar) in wallets/explorers, and transferring it hands over
  memory-write rights.
- Memory panel showing live facts/summary and the 0G Storage root.

### Changed
- Redeployed the contract to `SoulAgentV2` (`0x04188499…7ED0`) on 0G Galileo.

## v0.1 - Group stage (MVP)

### Added
- Create an AI companion, chat with it via 0G Compute, persist its memory to 0G
  Storage, and mint it as an ERC-721 on the 0G chain.
- Reload-and-remember: open `/agent/<id>` from any browser and the memory is pulled
  back from 0G Storage - nothing stored on the server.
- Hybrid access: anyone can chat; only the token holder can save new memories on-chain.
- CRT-terminal UI (phosphor green, scanlines, glitch, boot sequence).
